# 가정 및 리스크

## TL;DR
- 제품/현장 운영 특성상 **서명/사진/네트워크/오프라인** 리스크가 핵심입니다.

## 본문
Phase 1 가정 및 리스크 (Assumptions & Risks) v0.1
1) 가정(Assumptions)
1. 사용자는 3역할(Admin/TM/Technician)로 시작하며, 권한은 role 기반으로 충분하다.
2. 기사 현장 업무는 모바일 웹 사용을 기본으로 한다(태블릿/폰).
3. Phase 1은 오프라인(무신호) 동기화 없이도 운영 가능하다고 가정한다.
4. 체크리스트 항목은 Phase 1 동안 큰 구조 변경이 잦지 않다(문구 미세조정 수준).
5. 사진 업로드는 Phase 1에서 “권장”이며, 완료 조건에 강제하지 않아도 필드테스트가 가능하다.
6. PDF는 “완료 증빙 보관”이 목적이며, 디자인/서식 고도화는 Phase 2로 미룬다.

2) 리스크(Risks) 및 대응(초기 방침)
1. 네트워크 불안정으로 autosave/업로드 실패 가능
→ 실패 · 재시도 UX, 임시 로컬 저장(선택), 업로드 재시도 버튼 제공
2. 서명 UX 실패(고객이 잘못 입력/서명 누락/저장 실패)
→ 서명 섹션을 완료 제출의 강제 조건으로 두고, 실패 시 해당 섹션으로 자동 이동
3. 현장 데이터 입력 누락(FAIL 메모 미작성, 필수 항목 미체크)
→ 완료 제출 시 서버 검증 + 누락 항목 하이라이트/자동 스크롤
4. 배정 정책 혼선(본사가 기사까지 배정하거나 팀이 임의로 일정 변경)
→ RBAC 고정: Admin=팀, TM=기사, 상태별 수정 제한 + 사유 로그(변경 시)
5. PDF 생성/저장 실패(서버 부하/렌더링 오류/파일 저장 오류)
→ “PDF 준비됨/실패” 상태 표시 + 재생성(옵션) + 최소 대체(HTML 인쇄)
6. 발송 실패(번호/이메일 오류, 공급자 장애)
→ Phase 1은 발송을 부가 기능으로 취급, 재발송은 Admin만, 실패 사유 최소 기록
7. 개인정보/보안 리스크(연락처/서명 이미지/현장 주소)
→ 스코프(ORG/TEAM/SELF) 엄격 적용, 다운로드 링크 만료, 접근 로그 최소 기록
8. 외주/팀 인계 시 문서-구현 불일치
→ Screen Index + 상태 모델 + RBAC + In/Out Scope를 “단일 진실 소스”로 유지

3) Phase 1에서의 운영 원칙(짧게)
• “빠르게 굴러가는 것” 우선: 오프라인/고급 통계/재고는 Phase 2 이후
• “증빙 완결성” 우선: 체크리스트 + FAIL 메모 + 서명 = 완료 조건 고정
• “혼선 방지” 우선: 배정 체인(본사→팀→기사) 권한은 흔들지 않음

(본문 끝)
===
