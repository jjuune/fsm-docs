# 로그인 / 홈(역할별 랜딩)

## TL;DR
- **L-01 로그인:** 이메일/비밀번호 최소 UX, 에러 카피 정의
- **H-01 My Home:** `GET /me` 기반 역할별 랜딩 라우팅 (과투자 방지)

---

## 🔐 1. L-01 로그인 화면 (UX 최소 스펙)

### 화면 구성
| 요소 | 내용 |
| :--- | :--- |
| 제목 | 로고 + "로그인" 텍스트 |
| 입력 필드 | 이메일 (type=email) / 비밀번호 (type=password) |
| 액션 버튼 | "로그인" (primary) |
| 부가 링크 | "비밀번호를 잊으셨나요?" (선택, Phase 1) |

### 에러 카피 (서버 응답 기준)
| 상황 | UI 메시지 |
| :--- | :--- |
| 이메일 형식 오류 | "올바른 이메일 형식을 입력해주세요." |
| 이메일/비밀번호 불일치 | "이메일 또는 비밀번호가 올바르지 않습니다." |
| 비활성화 계정 | "비활성화된 계정입니다. 관리자에게 문의하세요." |
| 서버 오류 | "일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요." |

> **Phase 1:** OTP/소셜 로그인은 제외. 이메일+비밀번호만.

---

## 🏠 2. H-01 My Home (역할별 랜딩 라우팅)

### 목적
`GET /me` 응답의 `role` 값을 기준으로 로그인 직후 적절한 화면으로 자동 이동합니다.
H-01 자체는 별도 UI가 아닌 **라우팅 레이어** 역할입니다.

### 역할별 랜딩
| role | 랜딩 화면 | 설명 |
| :---: | :--- | :--- |
| `ADMIN` | A-00 (Admin 대시보드) | 팀별 운영지표 MVP 카드 |
| `TM` | T-00 (팀 대시보드) | 기사별 운영지표 MVP 카드 |
| `TECH` | M-01 (기사 작업 목록) | 본인 배정 작업 목록 |

> 세션 만료/토큰 무효 시 → L-01 로그인 화면으로 리다이렉트.
> **과투자 방지:** H-01은 별도 홈 피드/알림 피드 없음. 최소 라우팅만.

---

## 🔗 3. `GET /me` Endpoint 정의

| 항목 | 내용 |
| :--- | :--- |
| Method | `GET` |
| Path | `/api/v1/me` |
| Auth | Bearer Token 필수 |
| Role | ALL (ADMIN / TM / TECH) |
| Scope | SELF |

**응답 예시:**
```json
{
  "ok": true,
  "data": {
    "id": "uuid",
    "email": "user@example.com",
    "name": "홍길동",
    "role": "TM",
    "team_id": "uuid",
    "team_name": "서울 북부 센터",
    "status": "ACTIVE"
  }
}
```

> `status: INACTIVE`인 경우 서버가 401을 반환하여 로그인 차단.

---

## 연관 문서
- [Phase 1 SSoT](00-phase1-functional-spec.md)
- [권한 매트릭스(RBAC)](00-rbac.md)
- [API 최소세트](../30-implementation/10-api-minimum-set-phase1.md)

## 변경 이력
- **v0.2:** 신규 작성 — L-01 로그인 UX, H-01 역할별 랜딩 라우팅, GET /me 정의 (2026-02-23)
